{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "arthas-onboarding",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "First-run onboarding wizard for Arthas. Walks the operator through provider selection and credential capture, then hands the writes off to opencode's auth store.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "exports": {
    ".": "./src/index.ts",
    "./wizard": "./src/wizard.ts",
    "./store": "./src/store.ts",
    "./validate": "./src/validate.ts"
  },
  "dependencies": {
    "@clack/prompts": "1.0.0-alpha.1",
    "arthas-theme": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
